
import { drizzle } from "drizzle-orm/d1"; // This won't work directly in node script without binding... 
// actually I should use simple SQL via wrangler d1 execute
console.log("Use wrangler d1 execute to seed users");
